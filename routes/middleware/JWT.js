/* const jwt = require('jsonwebtoken');
const secret = 'my super secret';
const data = { userId: 123, name: 'User 123' };
let token = jwt.sign(data, secret);
jwt.verify(token, secret);
jwt.verify(token, 'other secret'); */